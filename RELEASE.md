# Release Guide

Run `pytest -q`, `python tests/smoke_test.py`, regenerate data and reference outputs, execute all notebooks, regenerate the one-page visual, scan for secrets, confirm no em dash characters, and verify README claims. Suggested v0.1.0 text is in README.md.
