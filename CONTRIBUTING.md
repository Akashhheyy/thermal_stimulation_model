# Contributing

Install `.[dev]`, create a focused branch, run tests and the complete example, and update documentation with behavior changes. New models require equations, units, parameter bounds, references, sanity tests, invalid-input tests, and validation limitations. Never commit credentials or unauthorized data.
